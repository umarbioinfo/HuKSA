# HuKSA — pinned dependencies (analysis + app). Tested toolchain; versions match the
# environment the results were produced on. Install:  pip install -r requirements.txt
numpy==2.3.5
pandas==2.3.3
scipy==1.16.3
scikit-learn==1.8.0
matplotlib==3.10.8
rdkit==2025.9.3

# web app (execution UI)
streamlit==1.52.2
plotly==6.5.0
streamlit-ketcher==0.0.1
